generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Food {
    id       Int      @id @default(autoincrement())
    name      String
    description String?
    price       Float
    category     String
    available Boolean 
    createdAT DateTime @default(now())

    @@map("food")
}
